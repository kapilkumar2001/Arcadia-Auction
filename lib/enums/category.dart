enum PlayerCategory {
  gold,
  silver,
  bronze,
  unassigned,
}

// extension on PlayerCategory {
//   int compareTo(PlayerCategory other) => this.index.compareTo(other.index);
// }

// let areEqual = PlayerCategory.bronze < PlayerCategory.gold
